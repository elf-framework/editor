---
title: ELF framework 
---

import {Grid, Divider, Column, Blank} from "@elf-framework/ui";

import {ProjectItem} from "../../component/project/ProjectItem.jsx";
import {data} from "./data.jsx";

<div style={{fontSize: 40, padding: "30px 0px", fontWeight: 700}}>ELF Framework</div>

<div style={{fontSize: 20}}>An easy and customizable set of developers</div>

<Grid class="project-item-list" columns={3} style={{gap: 20, marginTop: 30}}>
    <ProjectItem title={"With elf-framework"} description={[
    <div>Open source developer,</div>,
    <div>&nbsp;</div>,
    <p>Let's get together in one place, study together, play together, and build together.</p>
    ]} docLink="/pages/introduction/" codeLinkText="Github" codeLink="https://github.com/elf-framework" />
</Grid>

<Divider margin={50}/>

<Grid class="project-item-list" columns={3} rows={1} style={{gap: 20}}>
    {data.map(it => {
        return <ProjectItem full title={it.title} npm={it.npm} description={it.description} docLink={it.docLink} codeLinkText={it.codeLinkText} codeLink={it.codeLink} type={it.type} selected={it.selected} />
    })}
</Grid>


<Blank style={{height: 100}} />

