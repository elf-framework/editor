{
  "pipeline": {
    "ui:dev": {
      "dependsOn": [
        "base-editor#editor:watch", 
        "^watch"
      ],
      "outputs": ["dist/**", ".next/**"],
      "cache" : false
    },
    "build": {
      "dependsOn": ["ui#build", "^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    }
  }
}
